{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Run Firefox",
        "type": "firefox",
        "request": "launch",
        "url": "http://localhost:3000",
        "webRoot": "${workspaceFolder}"
      }
    ]
  }